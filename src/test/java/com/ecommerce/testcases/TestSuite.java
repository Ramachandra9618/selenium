package com.ecommerce.testcases;

import org.junit.platform.suite.api.*;

@Suite
@SelectClasses({ LandingPageTest.class})
public class TestSuite {
    // No additional code needed; the annotations handle the suite execution
}
